import Create from './create';
import List from './list';

const ScheduleConfig = {
    sourceCreate: {
        name: 'Create',
        component: Create
    },
    list: {
        name: 'Create',
        component: List
    }
}

export default ScheduleConfig;
