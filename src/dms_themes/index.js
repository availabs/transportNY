import {defaultTheme} from './default_theme'
import transportny from './transportny/theme'
import fullmap from './fullmap_theme'

const themes = {
  //"default": {},//defaultTheme,
	fullmap,
	transportny
}

export default themes
